# ── AI Doctor — Frontend Environment ──────────────────────────────────
# On Vercel, set this in Project → Settings → Environment Variables.
# Injected into js/config.js at build time by build.mjs.

# Base URL of the deployed backend API (no trailing slash).
# Leave empty for same-origin local dev.
API_BASE_URL=https://ai-doctor-virtual-medical-assistant.onrender.com
